/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["upload.wikimedia.org", "www.flagcolorcodes.com"]
    },
    output: "export"
}

module.exports = nextConfig
